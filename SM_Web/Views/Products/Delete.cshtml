@model SM_Web.Models.Product

@{
    ViewData["Title"] = "Delete Product";
}

<div class="container-fluid">
@*     <!-- Breadcrumb -->
    <div class="page-header">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Dashboard</a></li>
                <li class="breadcrumb-item"><a asp-controller="Products" asp-action="Index">Products</a></li>
                <li class="breadcrumb-item active" aria-current="page">Delete @Model.Name</li>
            </ol>
        </nav>
    </div> *@

    <!-- Enhanced Breadcrumb -->
    <div class="page-header mb-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb bg-transparent px-0 mb-0">
                <li class="breadcrumb-item fs-sm">
                    <a asp-controller="Home" asp-action="Index" class="text-decoration-none d-flex align-items-center">
                        <i class="fas fa-tachometer-alt me-2 text-primary"></i>
                        <span class="text-primary">Dashboard</span>
                    </a>
                </li>
                <li class="breadcrumb-item fs-sm">
                    <a asp-controller="Products" asp-action="Index" class="text-decoration-none d-flex align-items-center">
                        <i class="fas fa-boxes me-2 text-primary"></i>
                        <span class="text-primary">Products</span>
                    </a>
                </li>
                <li class="breadcrumb-item fs-sm active d-flex align-items-center" aria-current="page">
                    <i class="fas fa-trash-alt me-2 text-muted"></i>
                    <span class="text-muted">Delete @Model.Name</span>
                </li>
            </ol>
        </nav>
    </div>

    <!-- Page Header -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-danger">Delete Product</h1>
    </div>

    <!-- Delete Confirmation Card -->
    <div class="card shadow mb-4">
        <div class="card-header py-3 bg-danger text-white">
            <h6 class="m-0 font-weight-bold">Confirm Deletion</h6>
        </div>
        <div class="card-body">
            <div class="alert alert-warning">
                <strong>Warning!</strong> Are you sure you want to permanently delete this product?
            </div>

            <div class="product-details">
                <div class="detail-item">
                    <span class="font-weight-bold">Code:</span>
                    <span>@Model.Code</span>
                </div>
                <div class="detail-item">
                    <span class="font-weight-bold">Name:</span>
                    <span>@Model.Name</span>
                </div>
                <div class="detail-item">
                    <span class="font-weight-bold">Price:</span>
                    <span class="font-weight-bold">$@Model.Price</span>
                </div>
                <div class="detail-item">
                    <span class="font-weight-bold">Stock Quantity:</span>
                    <span>@Model.StockQuantity</span>
                </div>
            </div>

            <form asp-action="Delete" method="post" class="mt-4">
                <input type="hidden" asp-for="Id" />
                <div class="text-right">
                    <a asp-action="Index" class="btn btn-secondary mr-2">
                        <i class="fas fa-arrow-left mr-2"></i> Cancel
                    </a>
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash-alt mr-2"></i> Confirm Delete
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Styles {
    <style>
        .product-details {
            background-color: #f8f9fa;
            padding: 1.5rem;
            border-radius: 0.35rem;
            border: 1px solid #e3e6f0;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px solid #e3e6f0;
        }

            .detail-item:last-child {
                border-bottom: none;
            }

        .alert-warning {
            border-left: 4px solid #f6c23e;
        }

        .card-header.bg-danger {
            background-color: #e74a3b !important;
        }
    </style>
}